<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- menu categories -->

        <!-- actions -->
        <record model="ir.actions.act_window" id="hr_recruitment.hr_applicant_resumes">
            <field name="name">All Resumes and Letters</field>
            <field name="res_model">ir.attachment</field>
            <field name="view_type">form</field>
            <field name="view_mode">kanban,tree,form</field>
            <field name="view_id" eval="False"/>
            <field name="search_view_id" ref="hr_recruitment.ir_attachment_view_search_inherit_hr_recruitment"/>
            <field name="domain">[('res_model','=','hr.applicant')]</field>
            <field name="context">{'create': False}</field>
            <field name="help" type="html">
                <p>
                    No document yet.
                </p><p>
                    This menu helps you search through resumes and motivation
                    letters. Odoo automatically indexes .PDF, .DOC, DOCX, .TXT
                    files so that you can search keywords from the content of these
                    files.
                </p>
            </field>
        </record>

        <record model="ir.actions.act_window" id="oit_hr_applicant_resumes">
            <field name="name">Resumes and Letters</field>
            <field name="res_model">ir.attachment</field>
            <field name="view_type">form</field>
            <field name="view_mode">kanban,tree,form</field>
            <field name="view_id" eval="False"/>
            <field name="search_view_id" ref="hr_recruitment.ir_attachment_view_search_inherit_hr_recruitment"/>
            <field name="domain">['&amp;',('res_model','=','hr.applicant'),('res_id', '=', active_id)]</field>
            <field name="context">{'create': False}</field>
            <field name="help" type="html">
                <p>
                    No document yet.
                </p><p>
                    This menu helps you search through resumes and motivation
                    letters. Odoo automatically indexes .PDF, .DOC, DOCX, .TXT
                    files so that you can search keywords from the content of these
                    files.
                </p>
            </field>
        </record>

        <!-- views -->
        <record model="ir.ui.view" id="oit_crm_case_form_view_job">
            <field name="name">Custom - Jobs - Recruitment Form</field>
            <field name="model">hr.applicant</field>
            <field name="inherit_id" ref="hr_recruitment.crm_case_form_view_job"/>
            <field name="arch" type="xml">
                <xpath expr="/form/sheet/group/group[4]/div[1]/field[@name='salary_expected']" position="before">
                    <field name="salary_visible" invisible="1"/>
                </xpath>
                <xpath expr="/form/sheet/group/group[4]/label[1]" position="attributes">
                    <attribute name="attrs">{'invisible':[('salary_visible','=',False)]}</attribute>
                </xpath>
                <xpath expr="/form/sheet/group/group[4]/div[1]" position="attributes">
                    <attribute name="attrs">{'invisible':[('salary_visible','=',False)]}</attribute>
                </xpath>
                <xpath expr="/form/sheet/group/group[4]/label[2]" position="attributes">
                    <attribute name="attrs">{'invisible':[('salary_visible','=',False)]}</attribute>
                </xpath>
                <xpath expr="/form/sheet/group/group[4]/div[2]" position="attributes">
                    <attribute name="attrs">{'invisible':[('salary_visible','=',False)]}</attribute>
                </xpath>
            </field>
        </record>

    </data>
</odoo>