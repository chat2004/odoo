<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="legacy_projects.list">
      <field name="name">legacy_projects list</field>
      <field name="model">legacy_projects.legacy_projects</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->
    <record model="ir.ui.view" id="legacy_projects.form">
      <field name="name">Legacy Project Form</field>
      <field name="model">project.project</field>
      <field name="inherit_id" ref="project.edit_project" />
      <field name="arch" type="xml">
        <xpath expr="/form/sheet/notebook/page[1]/group[1]/field[@name='partner_id']" position="after">
          <field name="legacy_projects_id" />
        </xpath>
      </field>
    </record>

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="legacy_projects.action_window">
      <field name="name">legacy_projects window</field>
      <field name="res_model">legacy_projects.legacy_projects</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="legacy_projects.action_server">
      <field name="name">legacy_projects server</field>
      <field name="model_id" ref="model_legacy_projects_legacy_projects"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="legacy_projects" id="legacy_projects.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="legacy_projects.menu_1" parent="legacy_projects.menu_root"/>
    <menuitem name="Menu 2" id="legacy_projects.menu_2" parent="legacy_projects.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="legacy_projects.menu_1_list" parent="legacy_projects.menu_1"
              action="legacy_projects.action_window"/>
    <menuitem name="Server to list" id="legacy_projects" parent="legacy_projects.menu_2"
              action="legacy_projects.action_server"/>
    -->
  </data>
</odoo>